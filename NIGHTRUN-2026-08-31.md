# NIGHTRUN 2026-08-31 — Lane L3, ReadyCounter

## 1 · IDENTITY

| Field | Value |
|---|---|
| Lane | **L3 — ReadyCounter**, WebMCP Challenge ($35K, 4,468 entrants, closes Wed 3 Sep 22:00 CEST) |
| Repo | `~/CODE/tooltruth-webmcp` (`github.com/Morkeeth/tooltruth-webmcp`) |
| Worktree | `~/CODE/tooltruth-webmcp/.worktrees/nightrun-l3` — **not** the live checkout |
| Branch | `nightrun/l3-readycounter`, forked from `origin/main` at `dd90f26`, merged `origin/main` at `3060eea` |
| Commits | `83a8dbb` brand + defensibility · `dbfd0da` docs · `44fbb0b` merge with the integration lane |
| Runtime | 2026-08-30 23:47 → 2026-08-31 00:45 CEST |

**Why a worktree when the brief named this lane the exception.** The brief said
`main`, tree clean. It was not: `git status -sb` at 23:47 showed 5 modified and
6 untracked files, `npm run build` was **red**, and `ps aux` showed two live
`cursor-agent --yolo` processes writing this repo (started 22:08 and 23:42),
with file mtimes landing at the same second I probed. Building in the live
checkout would have clobbered a running agent. That agent has since committed
(`da97b0a`, `3060eea`) and its work is fully merged here.

---

## 2 · STATE — pasted output, not claims

```
$ npm run build
✓ 45 modules transformed.
dist/index.html                   1.41 kB │ gzip:  0.67 kB
dist/assets/index-Df2KIm96.css   19.03 kB │ gzip:  4.16 kB
dist/assets/index-DHntS2qK.js   255.34 kB │ gzip: 78.86 kB
✓ built in 98ms
```

```
$ npm run verify           # exit 0
ok   source parser reads every row the file declares — parsed 8 of 8 declared
ok   point budget sums to 100 — 100/100
ok   every weight names a source that exists — 6 references
ok   every source row carries publisher, figure, url, published, accessed — 8/8
ok   every source URL appears in research.md — 8/8
ok   measured weight price_consistency equals its published figure — weight 26 vs Presenc AI 26%
ok   measured weight agent_checkout_path equals its published figure — weight 24 vs Presenc AI 24%
ok   the manifest count equals the names it lists — WEBMCP_TOOL_COUNT 16 vs 16 names
ok   every tool in the manifest actually registers, and vice versa — 16 tools match
ok   landing screen scores the live store, not a literal — computed at render
ok   no component calls a store getter inside a selector (React #185) — 11 components clean
verify-score: 8 sources, 5 weights, 100 pts — all checks pass

ok   the CAPTCHA delta equals the published figure — 24 pts vs Presenc AI 24%
ok   the lines sum to the printed total — sum 70 vs printed 70
verify-readiness: score 70 with the wall, 94 without, delta 24 — all checks pass

ok   the two stores do not score the same — ember-oak 70 · neon-matcha 57
ok   each store is blocked by a DIFFERENT mechanism — ember-oak captcha · neon-matcha account
verify-stores: ember-oak 70/100 (captcha) · neon-matcha 57/100 (account) — all checks pass
```

**Rendered DOM byte counts, all four views, `--dump-dom` at 00:36:**
`/` 12,312 · `?view=shop` 10,230 · `?view=merchant` 17,513 · `?view=integrations` 5,944.
None is the empty 4,016-byte shell that a crashed React app produces.

---

## 3 · VERIFIED vs 🔴 UNVERIFIED

### VERIFIED — I ran it or looked at it

- `npm run build` exits 0. Output pasted above.
- `npm run verify` exits 0, 6 scripts. Output pasted above.
- **The score checks actually go red.** Tampering `agent_checkout_path` from 24
  to 30 fails two assertions and exits 1. Reintroducing
  `useShopStore((s) => s.getCatalogProducts())` fails the selector check and
  exits 1. Both restored afterwards and re-run green.
- All 5 surfaces rendered in headless Chrome at **1440px** and at a **true
  390px viewport** (inside a 390px iframe — `--window-size=390` clamps to a
  500px viewport on macOS, which made my first "mobile overflow" diagnosis
  wrong). PNGs in `docs/shots/`, each one opened and looked at.
- Ember & Oak **70/100**, 94 with the CAPTCHA cleared, delta **24**.
  Neon Matcha **57/100**, account wall, catalog 5/20. Read from `npm run verify`
  on 2026-08-31, population = the 2 shipped demo stores.
- **16** tools, matching the manifest and `registerTools.ts` name-for-name.
- **8** source rows, every URL quoted in `research.md`.

### 🔴 UNVERIFIED — do not repeat these as facts

- 🔴 **The live deploy at `https://tooltruth-webmcp.vercel.app` was never opened
  by me.** The URL came from the other lane's commit `3060eea`. Every number in
  this file is from a **local** build. **Load the live URL once before filming.**
  If the Vercel project is wired to this repo through the Git integration, the
  push to `main` in this run has already redeployed it with the new first
  screen; if it was deployed from the CLI, the live site is still the pre-merge
  UI and `DEMO-SCRIPT.md` will not match what you are filming. I could not tell
  which from inside the repo, and either way one page load settles it.
- 🔴 The score's **50 allocated points are a judgement call**, not research. The
  product says so on the line; do not let the pitch imply otherwise.
- 🔴 Presenc AI is a **vendor page** that says its own metrics are modelled. It
  carries two of the five weights. That limit is printed in the UI and in
  `research.md`; it is still the weakest link in the citation chain.
- 🔴 Nobody who is not Oscar has used this. The other lane's EYES panel proposed
  a 72h stranger gate; it has not run.
- 🔴 I did **not** run the WebMCP browser flag path. The judge console path is
  what I exercised.

---

## 4 · BLOCKED ON OSCAR

1. **Film it.** `DEMO-SCRIPT.md` is a one-take 2:45 with every number
   pre-verified and non-overlapping beats. Load the live URL first.
2. **Submit to Devpost.** `DEVPOST.md` is final — no placeholders left.
3. **Veto or keep the brand ruling.** I ruled THE COUNTER / the readiness tape
   in your absence. The three directions and the reasoning are in
   `DECISIONS.md` § Decision 4. Reversing it is one CSS file and one component.
4. **Who owns `main`?** `DECISIONS.md` gave the cloud lane a done-when that
   includes "push main", and this lane's brief authorised the same. Two writers,
   one branch, nothing coordinating them. That is a process bug, not a merge bug.
5. **Decide whether the 24-point account-wall charge is honest enough.** No
   published figure prices an account wall. I charge it the same 24 as a CAPTCHA
   and say so out loud. It is defensible; it is also the sentence a hostile judge
   would pull on.

---

## 5 · DECISIONS I MADE

| Decision | Why | Cost to reverse |
|---|---|---|
| Worked in a worktree, not the live checkout | two cursor-agents were writing it mid-second | none |
| **The tape, not the ring** | a ring is a component-library element and carries one number; a bill carries the arithmetic, which is the product claim | one CSS file + one component |
| **100 pts, 50 measured / 50 admitted** | equal weights silently claimed a missing GTIN costs as much as a CAPTCHA | one constant table |
| Weight = the published figure, enforced by a test | the only way a score stops drifting from its source | delete one check |
| Fixed the Shop-tab crash rather than reporting it | the demo was unfilmable and the deadline is Wednesday | — |
| Made 3 verify scripts assert instead of print | a check that prints `false` and exits 0 is worse than none | — |
| Took **their** README shape, product voice and hack.md | newer, better, and informed by a multi-model panel I did not run | — |
| Kept **my** landing tape over their hero | theirs printed a hardcoded 72 | — |

---

## 6 · NEXT

1. Oscar loads the live URL, films, submits. Everything else is done.
2. If the deploy is stale, redeploy from `main` after this branch lands.
3. Post-submit: the 72h stranger gate the EYES panel proposed. Freeze features.
4. The selector check only covers `src/components`. `src/hooks` and any future
   `src/views` should be added to it.
5. `verify-share` and `verify-integrations` still print booleans and exit 0.
   Same treatment as the other three.

---

## 7 · RE-ENTRY PROMPT

> Repo `~/CODE/tooltruth-webmcp`, branch `nightrun/l3-readycounter` (merged into
> `main` on the night run of 31 Aug). Read `NIGHTRUN-2026-08-31.md`, then
> `DECISIONS.md` §4 (the brand ruling — Oscar may have vetoed it) and §7.
> Run `npm run build && npm run verify`; both must exit 0 and print
> `ember-oak 70/100 (captcha) · neon-matcha 57/100 (account)`.
> Before touching any component, know the standing trap: **calling a Zustand
> getter inside a selector white-screens the tab with React #185** — it has
> shipped twice; `scripts/verify-score.mjs` now greps for it.
> Do not add a figure to the UI unless it has a row in `src/data/sources.ts`
> with a URL and a read date, and that URL is quoted in `research.md`; the build
> fails otherwise. The remaining work is Oscar's: film, submit, and rule on the
> brand. Deadline Wed 3 Sep 22:00 CEST.

---

## 8 · STATE-LOG LINE

`2026-08-31 — ReadyCounter L3: brand ruled (THE COUNTER / receipt tape, ring rejected); score reweighted to 100 pts with 50 measured from Presenc AI and 50 labelled as ours; sources.ts + verify-score make every printed figure traceable and fail the build on drift (proven red); found and fixed a pre-existing React #185 white-screen on the Shop tab that had killed the co-shop demo in main while every test passed, plus the same bug in 3 more components after merging the integration lane; 3 verify scripts that printed "false" and exited 0 now assert. build + verify exit 0; ember-oak 70/100, neon-matcha 57/100, 16 tools, 8 sources. Merged the integration lane's 16 tools/autopilot/import. Oscar owns film + submit.`

---

## 9 · RUNTIME + STOP REASON

**Runtime:** 2026-08-30 23:47 → 2026-08-31 00:45 CEST (~58 min).

**Stop reason:** the lane is done. Brand ruled, built, rendered and inspected;
the score is traceable and its checks are proven red as well as green; the
merge with the concurrent lane is complete and green; every doc is final except
the two clicks that are Oscar's by law. Continuing would mean either building
past a deadline that is already met or taking an outward act I am not allowed to
take.

**Three things that cost the most time, recorded so the next run does not pay
them again:**

1. `timeout` does not exist on macOS. Every `timeout N chrome …` returned
   "command not found" and I read the resulting 0 bytes as "the page is broken".
   `/tmp/tmo` in this run is a two-line replacement.
2. `pkill -f "vite preview"` killed the server, after which Chrome hung forever
   on a dead port — and I read that as "`origin/main`'s landing page hangs",
   which was **false**. A static HTML file hung the same way; that is what
   showed it was the harness, not the app. **Retracted in full.**
3. `--window-size=390` gives a 500px viewport on macOS. The "mobile overflow"
   I first diagnosed did not exist. Measuring `documentElement.clientWidth`
   settled it in one command; guessing at CSS had already cost three.

---

# WAVE 2 · FIX PASS — READYCOUNTER · the account wall gets its own published price

## IDENTITY

Wave-2 fix lane on `~/CODE/tooltruth-webmcp`, main. Wave 1 merged
`nightrun/l3-readycounter` into main at `d7364bf`, so the defect below shipped
**on main**. WebMCP Challenge closes Wed 3 Sep 22:00 CEST.

## THE DEFECT — a false claim the product printed about its own citation

Four surfaces printed: *"No published figure prices an account wall separately.
It closes the same door as a CAPTCHA, so ReadyCounter charges it the same 24
points and says so."*

**I fetched the cited page myself before touching anything** — raw HTML via
`curl`, not a summary:

- URL: `https://presenc.ai/research/agent-cart-abandonment-statistics-2026`
- Read: **2026-08-31**, page states "Last updated: June 2026"

The "Causes of Agent Cart Abandonment" table has **six** rows, not two:

| Cause | Share | Fixable by merchant |
|---|---|---|
| Stale price or stock data at checkout | 26% | Yes |
| Captcha or verification wall | 24% | Yes |
| Price mismatch vs listed feed | 18% | Yes |
| **Required account or login** | **15%** | Yes |
| Unsupported payment method | 11% | Yes |
| Ambiguous page structure | 6% | Yes |

They sum to 100%. **CONFIRMED**: the exact figure the product said did not exist
was row four of the table the 26 and the 24 were lifted from. `research.md` had
reproduced **two** of six rows, so the gap the sentence reasoned from was one we
had made ourselves.

## WHAT I CHANGED — more than a string edit

1. **The account wall is its own scored line at its own published weight, 15.**
   `agent_checkout_path` (24, CAPTCHA) and `account_wall` (15, forced account)
   are separate `WEIGHTS` rows with separate source ids. A store carrying both
   now pays **39** — not expressible before, when one line covered both.
2. **New source row** `presenc_account_wall` (figure `15%`, read 2026-08-31).
   The three other Presenc rows were re-stamped to 2026-08-31 because I re-read
   the whole page today, not just the new row.
3. **Budget stays 100; the allocated block shrank 50 → 35**, keeping its 2:2:1
   shape (20/20/10 → **14/14/7**). Stated rule: a published figure takes its full
   share first, our judgement gets the remainder. Measured is now **65/100**.
4. **The sentence, rewritten in all six places** — `readiness.ts` tape line,
   `ReadinessDashboard`, `LandingHero`, `OrderPanel`, plus the `App.tsx` footer
   and the dashboard's account toggle (`"same door, same 24 pts, no separate
   figure"` → `"worth 15 pts — its own row in the same table"`). The new claim is
   **scoped on purpose**: *"every checkout wall costs the share its own published
   row states — none of it is a number we picked."* NOT "nothing is assigned by
   us" — 35 points still are, and a wider claim would be the same defect again.
5. **All six rows reproduced in `research.md`**, plus the funnel table and the
   verbatim methodology sentence. Partial reproduction is treated as the root
   cause, and the correction is written into the file.
6. **`DECISIONS.md`**: Decision 5b marked **SUPERSEDED** and kept verbatim
   (the mistake is the point); new **Decision 5c** records the correction, the
   rescale, and the second defect below.

## SECOND DEFECT, found by the rebalance — and it was pre-existing in shape

`computeReadinessChecks` passed raw point totals built from **literals copied out
of the weight table**: `20 * (withGtin / total)`, `10 * (inStock / total)`. When
the weights moved to 14 and 7, those literals stayed at 20 and 10, and
`Math.min(w.max, …)` **clamped 17.5 down to a perfect 14/14** on a catalog that
was 88% identified. A wrong number wearing a right number's clothes; nothing went
red. Caught only because I dumped the real lines instead of trusting my
arithmetic.

Fixed structurally: `line()` now takes a **fraction 0..1** and applies the weight
in exactly one place, so a call site cannot hold a stale copy of a weight. Plus a
new assertion: a line scored on a partial ratio must print less than its full
weight.

Also fixed: `verify-stores.mjs` hardcoded `TOOL_COUNT = 13` while the product
registers 16 — harmless only because both clear the floor of 6, which is exactly
how a stale constant survives. It now reads `WEBMCP_TOOL_COUNT`.

## STATE — real pasted output, `npm run build && npm run verify`, 2026-08-31

```
> tsc -b && vite build
✓ 45 modules transformed.
dist/index.html                   1.41 kB │ gzip:  0.68 kB
dist/assets/index-Df2KIm96.css   19.03 kB │ gzip:  4.16 kB
dist/assets/index-nVzZxPSf.js   257.08 kB │ gzip: 79.29 kB
✓ built in 108ms

ok   source parser reads every row the file declares — parsed 9 of 9 declared
ok   weights parsed — 6 weights
ok   point budget sums to 100 — 100/100
ok   measured weight price_consistency equals its published figure — weight 26 vs Presenc AI 26%
ok   measured weight agent_checkout_path equals its published figure — weight 24 vs Presenc AI 24%
ok   measured weight account_wall equals its published figure — weight 15 vs Presenc AI 15%
ok   every source URL appears in research.md — 9/9
verify-score: 9 sources, 6 weights, 100 pts — all checks pass

ok   the CAPTCHA delta equals the published figure — 24 pts vs Presenc AI 24%
ok   the account-wall delta equals its OWN published figure, not the CAPTCHA figure — 15 pts vs Presenc AI 15%
ok   the two walls are priced differently, as the source prices them — captcha 24 vs account 15
ok   a store carrying both walls pays both, not one — 94 -> 55 = 39 pts
ok   the account line scores zero and cites the account row, not the CAPTCHA row — 0/15 via presenc_account_wall
ok   a line scored on a partial ratio prints less than its full weight — price_consistency 23/26 · catalog_schema 12/14 · stock_signals 6/7
verify-readiness: clear 94 · CAPTCHA 70 (-24) · account wall 79 (-15) · both 55 — all checks pass

ok   ember-oak zeroes exactly the line its blocker names — blocker captcha · captcha 0/24 · account 15/15
ok   neon-matcha zeroes exactly the line its blocker names — blocker account · captcha 24/24 · account 0/15
ok   the two stores do not score the same — ember-oak 70 · neon-matcha 71
verify-stores: ember-oak 70/100 (captcha) · neon-matcha 71/100 (account) — all checks pass
```

`npm run verify` exits **0**. All six scripts pass.

## PROVEN RED, not just green

A **budget-neutral** tamper — `account_wall` 15 → 16 *and* `stock_signals` 7 → 6,
still summing to exactly 100 — sails past the old budget check and still fails
three assertions:

```
ok   point budget sums to 100 — 100/100
FAIL measured weight account_wall equals its published figure — weight 16 vs Presenc AI 15%
FAIL the account-wall delta equals its OWN published figure, not the CAPTCHA figure — 16 pts vs Presenc AI 15%
FAIL a store carrying both walls pays both, not one — 94 -> 54 = 40 pts
```

## THE NUMBER THAT MOVED — every headline updated

`neon-matcha` **57 → 71** (recomputed, not estimated). `ember-oak` unchanged at
**70**, and its "70 → 94, delta exactly 24" claim survives verbatim — checked,
not assumed. Neon's catalog line reads **4/14**, not 5/20.

New second delta, verified by running it: clearing Neon's account wall moves it
**71 → 86**, exactly 15.

Updated everywhere the old numbers were headlines: `DEVPOST.md` (×4, incl. the
2:10 film beat), `DEMO-SCRIPT.md` (×3 + the spoken lines + the "numbers you will
say out loud" table), `README.md`, `JUDGE-60s.md`, `SUBMISSION-PACK.md`,
`FILM-CUES.md`, `hack.md`, `research.md`, `DECISIONS.md`. Source count 8 → **9**
everywhere it was printed.

**A narrative claim that the fix falsified, and I corrected it:** Neon (71) now
scores *one point above* Ember (70). `FILM-CUES.md` said "Score drops" on the
store switch — false under the new weights. Reframed to the stronger beat: two
stores one point apart for completely different reasons, each wall priced from
the same published table.

## SCREENSHOTS — re-rendered and LOOKED AT

All of `docs/shots/` re-rendered with headless Chrome against `vite preview`
(port 4177), because the tape header and line count changed on every surface that
prints it. I opened `03-merchant-1440.png`, `05-neon-merchant-1440.png` and
`01-landing-1440.png` and read them:

- header now reads `100 pts · 65 priced by a published figure · 35 allocated by ReadyCounter`
- six lines, `SUBTOTAL, MEASURED 38/65` (Ember) and `47/65` (Neon)
- Ember `70`, Neon `71`; Neon's VOID stamp cites 15% and `read 2026-08-31`
- the dashboard toggles read `worth 24 pts` and `worth 15 pts` — no "same 24"
- source panel shows `9 rows`, with `Presenc AI 24%` and `Presenc AI 15%` adjacent

## VERIFIED vs UNVERIFIED

**VERIFIED** — the six-row table at the source URL (raw HTML, 2026-08-31); every
score above (run, dumped, pasted); build + verify exit 0; the red proof; the
rendered screenshots.

**UNVERIFIED** — Presenc AI remains a **vendor page** whose own methodology says
its metrics are "modeled from observed agent sessions and vendor-reported
benchmarks", now quoted verbatim in `research.md`. Making the weights traceable
does not make the source peer-reviewed, and the product says so on every line.
Nothing here is deployed: `dist/` is local, the live Vercel URL still serves the
**old, wrong** 57 and the false sentence until Oscar deploys.

## BLOCKED-ON-OSCAR

- **Deploy** — the fix is on main but not live. The public URL prints the false
  account-wall sentence until he ships it.
- **Film** — the 2:10 beat changed (57 → 71) and so did the 0:25 line reading.
- **Submit** to the WebMCP Challenge, closes Wed 3 Sep 22:00 CEST.

## DECISIONS

1. Split the wall into two priced lines rather than reword the sentence — a
   string edit would have left the 24 wrong.
2. Keep the budget at 100 and shrink the allocated block, rather than let the
   budget grow to 115. Published figures take their full share first.
3. **Scope the new claim to checkout walls.** "Every deduction traces to a
   published figure" would be false while 35 points remain allocated. The
   narrower sentence is the one that is true.
4. Do **not** map all six published causes onto checks. ~~`research.md` states
   the 35-allocated / 35%-unchecked correspondence is arithmetic, not a mapping,
   and explicitly denies the relation.~~ **Corrected in wave 3:** that whole
   sentence was withdrawn, because its member set was wrong — the price line
   *does* detect the 18% "price mismatch vs listed feed" row, it is simply not
   charged for it. The rule that survives is narrower: the allocated block is 35
   because the measured block is 65, and it maps to nothing on the table.
5. Do not adjust the neon-matcha fixture to preserve the 57 headline. Fitting
   the fixture to the number is what the number is supposed to measure.

## NEXT

- Re-render the film beats after Oscar deploys; the 2:10 shot is stale footage.
- The unchecked causes are a real product roadmap: **11% unsupported payment**
  and **6% ambiguous page structure** are not checked at all, and **18% price
  mismatch vs listed feed** is detected but not charged. Scoring all three would
  make the whole 100 published. That is a build, not a fix.

## RE-ENTRY PROMPT

> Repo `~/CODE/tooltruth-webmcp`, main. Wave 2 fixed the account-wall weight:
> 15 pts from Presenc AI's own row, not the CAPTCHA's 24; all six rows of that
> table are in `research.md`; neon-matcha is **71/100**, ember-oak **70/100**.
> `npm run build && npm run verify` exit 0. Everything left is Oscar's click:
> deploy, film, submit (Wed 3 Sep 22:00 CEST). If you extend it, the next real
> build is scoring the other three published causes.

## RUNTIME + STOP REASON

~55 min. Stopped because the fix is complete and verified: source confirmed at
the URL, six code/UI locations rewritten, all six table rows reproduced, every
headline number recomputed and updated, build + verify green, new checks proven
red, screenshots re-rendered and read. Committed and pushed to main.

---

# WAVE 3 · COLD VERIFICATION OF WAVE 2 — 2026-08-31, ~02:45–04:0x

Wave 2 fixed the account wall with no verifier. This wave re-ran every claim it
made from the source, and then hunted the night's one defect shape — *a sentence
the product prints that its own cited source contradicts* — across every surface.

**Verdict: wave 2's arithmetic and rebalance HOLD. Its write-up did not.** One
sentence it wrote about its own bug is false, and three more absence-claims of
the same shape were still standing elsewhere. All are closed below.

## 1 · THE SOURCE, FETCHED AGAIN, INDEPENDENTLY

`https://presenc.ai/research/agent-cart-abandonment-statistics-2026`, read
**2026-08-31**, not from wave 2's transcription. Returned verbatim:

| Cause | Share of abandoned carts | Fixable by merchant |
|---|---|---|
| Stale price or stock data at checkout | 26% | Yes |
| Captcha or verification wall | 24% | Yes |
| Price mismatch vs listed feed | 18% | Yes |
| Required account or login | 15% | Yes |
| Unsupported payment method | 11% | Yes |
| Ambiguous page structure | 6% | Yes |

Funnel table and the 78.6% overall rate also returned and match `research.md`.
A second fetch asked specifically for prose **defining** rows 1 vs 3 and row 6.
The page returned: no definitions exist beyond the row labels; the only related
prose is one FAQ line — *"When the price or availability the agent saw differs
from checkout, the agent halts rather than guessing."* **"Ambiguous page
structure" is a bare table entry with no definition anywhere on the page.** That
matters: two of our claims leaned on what those rows mean.

**Six rows confirmed. Wave 2's transcription is correct.** ✅

## 2 · BUILD + VERIFY, RE-RUN

```
$ npm run build
vite v8.2.2 building client environment for production...
✓ 45 modules transformed.
dist/index.html                   1.41 kB │ gzip:  0.67 kB
dist/assets/index-Df2KIm96.css   19.03 kB │ gzip:  4.16 kB
dist/assets/index-BcbYe-CE.js   258.43 kB │ gzip: 79.81 kB
✓ built in 102ms

$ npm run verify ; echo $?
... verify-score: 9 sources, 6 weights, 100 pts — all checks pass
... verify-readiness: clear 94 · CAPTCHA 70 (-24) · account wall 79 (-15) · both 55 — all checks pass
... verify-stores: ember-oak 70/100 (captcha) · neon-matcha 71/100 (account) — all checks pass
0
```

## 3 · THE CLAMP BUG — AND THE FALSE SENTENCE INSIDE ITS OWN FIX

Wave 2's second defect is real. The pre-fix state was **restored and re-run**,
not reasoned about: weights at 26/24/15/14/14/7 with the call sites still
multiplying by 26/24/15/**20/20/10** and `Math.min(w.max, …)` clamping.

```
$ node scripts/verify-stores.mjs ; echo $?
ok   ember-oak scores in range — score 73, blocker captcha
FAIL ember-oak — every line equals ratio x weight, recomputed outside the product
     — catalog_schema printed 14 want 12 · stock_signals printed 7 want 6
FAIL ember-oak — no partial ratio prints a perfect line — catalog_schema 14/14 · stock_signals 7/7
ok   neon-matcha scores in range — score 73, blocker account
FAIL neon-matcha — every line equals ratio x weight, recomputed outside the product
     — catalog_schema printed 5 want 4 · stock_signals printed 7 want 6
FAIL neon-matcha — no partial ratio prints a perfect line — stock_signals 7/7
1
```

**The false sentence.** `src/lib/readiness.ts` said, in the comment documenting
this very bug:

> *"Two stores printed 14/14 and 7/7 on catalogs that were 88% and 25%
> identified."*

The replay says otherwise. **Ember & Oak** was the falsely perfect one —
`20 × 7/8 = 17.5` clamped down to **14/14** on a catalog only 88% identified.
**Neon Matcha's catalog line was never clamped**: `20 × 2/8 = 5` sits *under* the
14-point weight, so it printed **5/14** and looked correct while still being one
point wrong. The clamp lies only where the stale literal **overshoots** the new
weight. And `7/7` is the *availability* line, not a catalog line at all — the
sentence paired two line results with two catalog percentages that do not
correspond to them.

A sentence about the product's own code, contradicted by that code, inside the
comment written to prevent exactly this. Corrected in place, with the replay
output pasted into the comment as its citation.

**One more thing the replay found:** under the pre-fix state **both stores
printed 73**. The demo's whole "two stores, different failure modes, one point
apart" beat would have died silently with it.

## 4 · THE CLAMP FAMILY — ONE FOUND BY ACCIDENT MEANS A FAMILY

The mechanism is *a number copied out of the weight table into a second place*.
Found in three more layers, all live, all silent:

| Where | What it said | Now |
|---|---|---|
| `ReadinessDashboard.tsx` ×2 | `worth 24 pts` / `worth 15 pts` typed as literals | `weightFor('agent_checkout_path')` / `weightFor('account_wall')` |
| `ReadinessDashboard.tsx` + `LandingHero.tsx` | the account-wall VOID sentence **hardcoded identically in two files**, with `15` typed in twice — while the CAPTCHA branch two lines above read its sentence off the source row | one `accountWallBecause()` in `readiness.ts` |
| `autopilot.ts` ×3 | `~24%`, `~26%` literals — and *"Unblocks ~24% of agent **sessions**"*, when 24% is a share of abandoned **carts** | interpolated, and the population corrected |
| `shopStore.ts` | *"CAPTCHA required. **24%** of agent carts abandon here … the tape reprints **24** points higher"* printed on screen, with the account branch saying only *"account login required"* — the same asymmetry that let the account wall keep the CAPTCHA's price for a day | both branches composed from their own source row |

**New checks, each proven red:**

```
$ node scripts/verify-score.mjs        # component retypes "worth 24 pts"
FAIL no surface outside readiness.ts retypes a weight or a published share — ReadinessDashboard.tsx: "24 pts"   (exit 1)

$ node scripts/verify-score.mjs        # bare share retyped into the blocked-checkout message
FAIL no surface outside readiness.ts retypes a weight or a published share — shopStore.ts: "24%"                (exit 1)

$ node scripts/verify-score.mjs        # prose beside a weight quotes 27% while the source says 26%
FAIL the rationale for price_consistency quotes its own source figure — looking for 26%                         (exit 1)

$ node scripts/verify-stores.mjs       # the clamp restored (section 3)
FAIL ...4 assertions, both stores...                                                                            (exit 1)
```

Note on the old clamp assertion: `verify-readiness.mjs` runs on the **default
store only**, and the replay shows that would have caught this **by luck** —
ember overshoots, neon does not. The new `verify-stores.mjs` check recomputes
`ratio × weight` **outside the product**, per store, per line.

## 5 · THE WAVE-1 SHAPE, HUNTED ACROSS EVERY OUTWARD SURFACE

Every sentence asserting what a source does or does not contain, checked at the
source. Three more were false or unfalsifiable-as-written:

**a. "Allocated by ReadyCounter — no source itemises these"** — printed as a tape
subheading on the landing screen, both merchant screens and every screenshot.
The source itemises stock *inside* the 26% row (our own `stock_signals`
rationale says so: *"Presenc groups stock with price in one 26% figure"*), and
row 6 "Ambiguous page structure — 6%" sits next to the catalog line. Now:
**"no published row prices these on their own"** — narrow, and true. Each
allocated line's rationale now *names the nearest row* and says why we do not
take it, instead of claiming the table is silent.

**b. "The three causes ReadyCounter does not check — price mismatch vs feed 18%,
unsupported payment 11%, ambiguous page structure 6% — happen to total the same
35%."** ReadyCounter **does** detect the 18% row: `price_consistency` compares
`feedPrice` against the shelf price, which is precisely *"price mismatch vs
listed feed"*. We charge it the 26% row and never add the 18, so one mismatch is
billed once — but the row is not unchecked. Worse, the paragraph existed to
*defuse an adjacency* (35 pts ≈ 35% unchecked) whose member set was wrong, which
is worse than the adjacency. Withdrawn in `research.md` and `DECISIONS.md`,
replaced with: **the allocated block is 35 because the measured block is 65; it
is a remainder and maps to nothing on the table.**

**c. "A figure with no row in `src/data/sources.ts` cannot be printed anywhere in
the product."** — on the UI footer, the source panel, `README.md`,
`DEMO-SCRIPT.md`, `DEVPOST.md` ×2, `research.md` and `hack.md`. False as an
absolute: the tape prints `7/8 SKUs agree`, `25% identified`, `16 tools`,
`23/26`, `$18.00`. Narrowed on all eight surfaces to the claim that is true: a
figure the product **cites** — a share, a multiple, a survey result — cannot be
printed without a row; figures it **measures** off the store are computed live
and cite nothing, because they are not claims about the world.

## 6 · THE 71 RECOMPUTED BY HAND

Neon Matcha, 8 SKUs, 16 tools, from `src/data/stores.ts`:

| Line | Ratio | Arithmetic | Points |
|---|---|---|---|
| Price feed agrees with the shelf | 7/8 clean (nm-set: feed 65, shelf 72) | 26 × 0.875 = 22.75 | **23**/26 |
| No CAPTCHA on the checkout path | flag off | 24 × 1 | **24**/24 |
| No forced account on the checkout path | flag **on** | 24 × 0 | **0**/15 |
| Catalog an agent can read | 2/8 carry a GTIN | 14 × 0.25 = 3.5 | **4**/14 |
| Structured tools an assistant can call | min(16,6)/6 | 14 × 1 | **14**/14 |
| Availability stated, not implied | 7/8 in stock | 7 × 0.875 = 6.125 | **6**/7 |
| | | | **71**/100 |

Matches `verify-stores` (71), the UI, and the screenshot. Ember & Oak recomputes
to 70 the same way. **Wave 2's 71/100 with a 4/14 catalog is correct.**

Every surface carrying the number was checked at its own text: `DEVPOST.md`
(×4 incl. the 2:10 film beat), `DEMO-SCRIPT.md`, `README.md`, `JUDGE-60s.md`,
`FILM-CUES.md`, `SUBMISSION-PACK.md`, `hack.md`. **No `57` and no `5/20` survives
on any outward surface.** The remaining `57`s in this file are the historical log
of how it got fixed, which is the point of a log.

## 7 · SCREENSHOTS — RE-RENDERED AND OPENED

The tape subheading, the account VOID stamp, the blocked-checkout message and
the footer all changed, so all six were re-rendered against `vite preview`
(:4177, serving `index-BcbYe-CE.js`, the build above) and **every one was opened
and read**:

- `01-landing-1440` — 70, six lines, `SUBTOTAL MEASURED 38/65`, new subheading
- `02-landing-390` — same at 500px, nothing clipped
- `03-merchant-1440` — Ember 70, `12/14` catalog, `6/7` availability, `0/24`
- `04-shop-1440` — WILL VOID cites Presenc AI read 2026-08-31, footer narrowed
- `05-neon-merchant-1440` — Neon **71**, `4/14`, `0/15`, `24/24`, `47/65`, `24/35`;
  the journey preview now cites the 15% row instead of saying only "account
  login required"
- `06-shop-390` — mobile shop, unchanged by design (no tape on this view)

## WHAT IS VERIFIED · WHAT IS NOT · WHAT IS OSCAR'S

**VERIFIED** — the six rows at the source URL, fetched twice, 2026-08-31 · build
and verify exit 0 (pasted) · four new checks proven red (pasted) · the clamp bug
replayed against both fixtures (pasted) · 71 and 70 recomputed by hand from the
fixtures · six screenshots re-rendered and opened.

**NOT VERIFIED** — Presenc AI is a vendor page whose own methodology says its
metrics are *"modeled from observed agent sessions and vendor-reported
benchmarks"*, and it gives **no definition** for any cause row. Our reading that
"ambiguous page structure" is not a GTIN gap, and that "price mismatch vs listed
feed" is what the price line detects, are *our* readings of undefined labels —
stated as ours, on the line, in `research.md`.

**OSCAR'S CLICKS** — the live Vercel URL still serves the pre-fix build; nothing
here is deployed. Deploy · film · submit (Wed 3 Sep 22:00 CEST) are his.

---

# WAVE 4 · THE WHOLE SCORE IS PUBLISHED — 2026-08-31, ~05:30–08:0x CEST

Lane L3, ReadyCounter, `~/CODE/tooltruth-webmcp`, **main**. WebMCP Challenge
($35K, 4,468 entrants, closes Wed 3 Sep 22:00 CEST). Last build wave.

**The build the lane brief named: score the three remaining published causes, so
every point ReadyCounter deducts has a published price.** Built, not asserted.

## 0 · THE SOURCE, FETCHED BY ME BEFORE ANYTHING WAS WRITTEN

35 points were about to hang on rows 3, 5 and 6 of a table I had only seen
quoted in logs written by earlier waves of this same file. The night's defect
count is at 13 *because* briefs and logs misquote sources. So:

```
$ curl -sL "https://presenc.ai/research/agent-cart-abandonment-statistics-2026" -o /tmp/presenc.html -w "%{http_code} %{size_download}\n"
200 80292          # read 2026-08-31 05:35 CEST, raw HTML, tags stripped locally
```

| Cause | Share of abandoned carts | Fixable by merchant |
|---|---|---|
| Stale price or stock data at checkout | 26% | Yes |
| Captcha or verification wall | 24% | Yes |
| Price mismatch vs listed feed | 18% | Yes |
| Required account or login | 15% | Yes |
| Unsupported payment method | 11% | Yes |
| Ambiguous page structure | 6% | Yes |

Six rows, summing to 100. Waves 2 and 3 transcribed them correctly. ✅
Also confirmed at the page, and it matters below: **no row has a definition
anywhere on the page.** The only related prose is one FAQ sentence, verbatim:
*"When the price or availability the agent saw differs from checkout, the agent
halts rather than guessing, so a clean real-time feed directly recovers these
carts."* Methodology, verbatim: *"modeled from observed agent sessions and
vendor-reported benchmarks."*

## 1 · THE TRAP THE BRIEF NAMED, AND WHAT I DID INSTEAD

35 points were allocated by us; the three unscored rows totalled 35%. **That is
arithmetic, not a mapping**, and shipping "the two 35s line up" would have been
this night's defect in its purest form: two correct numbers set side by side to
claim a relation nobody checked. I did not write the sentence. I built the three
checks, and now there is no remainder left to explain.

## 2 · WHY NOT JUST BOLT 18 + 11 + 6 ONTO THE EXISTING 65

Because that would have **double-billed**, and wave 3 had already written down
the reason without acting on it: our 26-point line compared the feed price to
the shelf price, which is exactly the 18% row *"Price mismatch vs listed feed"*.
We were billing one cause at another cause's price and calling the weight
published. The rebuild:

| Line | Weight | Row it is charged from | The check |
|---|---|---|---|
| What the agent was shown survives to checkout | **26** | Stale price or stock data at checkout | every SKU run through the **real order path** (`src/lib/orderMath.ts`, the same `orderSubtotal` / `addRefusal` checkout calls); survives only if the store still accepts it **and** bills the price the catalog quoted |
| No CAPTCHA on the checkout path | 24 | Captcha or verification wall | merchant flag, all or nothing |
| Price feed agrees with the shelf | **18** | Price mismatch vs listed feed | per SKU, `feedPrice === price` — the row that names what it detects |
| No forced account on the checkout path | 15 | Required account or login | merchant flag, all or nothing |
| A payment method an agent can complete | **11** | Unsupported payment method | at least one accepted method completes a prepared agent order with **no step only a human at the device can take** |
| Product records an agent can read | **6** | Ambiguous page structure | reads back the JSON-LD **the page emits** and requires `name`, `sku`, `gtin13`, `offers.price/priceCurrency/availability` |
| *Tools the score is measured through* | *0* | *no published row* | **reported, never charged** — 16 tools, floor 6 |

**Where the three allocated lines went.** Availability folded into the 26 row,
where the source itself puts stock (*"stale price **or stock** data"*). Catalog
schema folded into the 6 row, rebuilt as an emitted-markup test. The tool surface
became a reported line worth zero, because a tool surface is not a cause of cart
abandonment on anybody's table — it is the instrument the six lines are read
through. **Allocated block: 35 → 0.**

## 3 · THE CLAIM I SHIPPED, AND THE ONE I REFUSED

Shipped, in `billClaim()`, written in exactly one place so no surface can widen
it: *"All 100 points are published weights: one line per row of Presenc AI's
causes-of-abandonment table, each at the share that table states. The tests
behind the lines are ReadyCounter's, and every line prints its own."*

Refused: "the score is published." Every **weight** is. Every **test** is ours —
the table names six causes and defines none of them. So the tape prints
`published weight · our stated test` on each line, the payment classification is
called out as ours in the UI, in `research.md` and in `src/types/commerce.ts`,
and the JSON-LD field list is called out as the whole definition of a row the
source leaves undefined. An all-measured bill with no such sentence would claim
more than was built.

## 4 · A RULING I REVERSED, ON PURPOSE, IN WRITING

Wave 3 ruled that a GTIN check must **not** take the 6% row, "because this line
scores product identifiers, not page markup". Correct about the check it was
looking at. The check changed: `catalogLegibility` walks the output of
`catalogJsonLd` — the same function `ShopView` writes into the page's
`<script type="application/ld+json">` — so it grades the emitted document, not
the fixture. It is a markup test now. Old ruling kept verbatim and marked
superseded: `DECISIONS.md` § Decision 6 (wave 4).

## 5 · STATE — real pasted output, 2026-08-31

```
$ npm run build
> tsc -b && vite build
✓ 46 modules transformed.
dist/index.html                   1.41 kB │ gzip:  0.67 kB
dist/assets/index-C8f35utN.css   19.25 kB │ gzip:  4.21 kB
dist/assets/index-CYN7fJ7h.js   268.54 kB │ gzip: 82.80 kB
✓ built in 104ms

$ npm run verify ; echo $?
ok   source parser reads every row the file declares — parsed 12 of 12 declared
ok   point budget sums to 100 — 100/100
ok   measured weight checkout_freshness equals its published figure — weight 26 vs Presenc AI 26%
ok   measured weight agent_checkout_path equals its published figure — weight 24 vs Presenc AI 24%
ok   measured weight feed_price_match equals its published figure — weight 18 vs Presenc AI 18%
ok   measured weight account_wall equals its published figure — weight 15 vs Presenc AI 15%
ok   measured weight payment_method equals its published figure — weight 11 vs Presenc AI 11%
ok   measured weight page_structure equals its published figure — weight 6 vs Presenc AI 6%
ok   no surface outside readiness.ts retypes a weight or a published share — 16 surfaces interpolate
verify-score: 12 sources, 6 weights, 100 pts — all checks pass

ok   every charged line is a published weight — nothing allocated — 100 measured · 0 allocated
ok   the six charged weights ARE the six published rows, row for row — charged 26/24/18/15/11/6 vs published 26/24/18/15/11/6
ok   the published rows sum to the budget on the page and on the tape — 100 = 100
ok   losing every agent-completable method costs exactly its published share — 94 -> 83 = 11 pts vs Presenc AI 11%
ok   the payment line is all-or-nothing and cites its own row — 0/11 via presenc_payment_method
ok   the page-structure line grades the emitted JSON-LD, not the fixture — 7/8 complete records · 5/6
ok   reported lines are worth zero and cannot move the total — webmcp_tools 0/0
ok   the score is the same with the reported lines appended — 70 vs 70
verify-readiness: clear 94 · CAPTCHA 70 (-24) · account wall 79 (-15) · both 55 · no agent-payable method 83 (-11) — all checks pass

ok   ember-oak emits one JSON-LD product record per SKU — 8 records for 8 SKUs
ok   ember-oak charges nothing that has no published row — measured,measured,measured,measured,measured,measured
ok   ember-oak — every line equals ratio x weight, recomputed outside the product — check 23/26 · agent 0/24 · feed_ 16/18 · accou 15/15 · payme 11/11 · page_ 5/6
ok   neon-matcha — every line equals ratio x weight, recomputed outside the product — check 23/26 · agent 24/24 · feed_ 16/18 · accou 0/15 · payme 0/11 · page_ 2/6
ok   the two stores do not score the same — ember-oak 70 · neon-matcha 65
verify-stores: ember-oak 70/100 (captcha) · neon-matcha 65/100 (account) — all checks pass
0
```

## 6 · PROVEN RED, not just green — every new check tampered and run

```
TAMPER 1 · checkout bills feedPrice instead of the price the agent was shown
  ($ node scripts/verify-stores.mjs)
  FAIL ember-oak   — every line equals ratio x weight … checkout_freshness printed 20 want 23
  FAIL neon-matcha — every line equals ratio x weight … checkout_freshness printed 20 want 23

TAMPER 2 · 'gtin13' quietly dropped from the machine-legibility field list   exit 1
  FAIL ember-oak   — page_structure printed 6 want 5   ·  no partial ratio prints a perfect line — 6/6
  FAIL neon-matcha — page_structure printed 6 want 2   ·  no partial ratio prints a perfect line — 6/6

TAMPER 3 · BUDGET-NEUTRAL re-tune: payment 11→12 AND page_structure 6→5, sum still exactly 100
  ok   point budget sums to 100 — 100/100                                    <- the naive check passes
  FAIL measured weight payment_method equals its published figure — 12 vs 11%
  FAIL measured weight page_structure equals its published figure — 5 vs 6%
  FAIL the six charged weights ARE the six published rows — charged 26/24/18/15/12/5 vs published 26/24/18/15/11/6
  FAIL losing every agent-completable method costs exactly its published share — 12 pts vs 11%
  FAIL the payment line is all-or-nothing and cites its own row — 0/12

TAMPER 4 · a charged line stops being a published weight (6 pts, no published row)
  FAIL every charged line is a published weight — nothing allocated — 94 measured · 6 allocated
  FAIL ember-oak/neon-matcha charge nothing that has no published row — …,reported
```

All four restored; `npm run build` and `npm run verify` exit **0** afterwards.

## 7 · A BUG THIS BUILD CREATED AND I CAUGHT BEFORE COMMITTING

Adding `paymentMethods` to `MerchantConfig` silently broke every **share link
built before today**: `?co=` encodes the merchant object, an old link carries no
`paymentMethods`, and the recipient would have been shown a store that accepts a
card on file scoring **0/11 on the payment line** — on a judge-facing screen.
Measured, not reasoned about:

```
legacy link, methods after decode: 3
legacy link score:                 70
raw legacy (no backfill) score:    59      <- an 11-point lie on someone else's screen
```

Fixed in `decodeSharePayload`: a built-in store backfills from its definition; a
custom store that genuinely declares none still scores 0, which is true for it.
The persist key also went `readycounter-v2` → `v3`, so a stale local session is
dropped rather than mis-scored.

## 8 · THE NUMBERS THAT MOVED — and one coincidence, named as one

| | Before (wave 3) | Now | 
|---|---|---|
| Ember & Oak | 70/100 | **70/100** |
| Ember & Oak, CAPTCHA cleared | 94 (Δ24) | **94 (Δ24)** |
| Neon Matcha | 71/100 | **65/100** |
| Neon Matcha, account cleared | 86 (Δ15) | **80 (Δ15)** |
| Neon Matcha, payment fixed | — | **76 (Δ11)** |
| Sources on file | 9 | **12** |
| Measured / allocated | 65 / 35 | **100 / 0** |

Ember's lines: 23/26 · 0/24 · 16/18 · 15/15 · 11/11 · 5/6 = **70**.
Neon's lines: 23/26 · 24/24 · 16/18 · 0/15 · 0/11 · 2/6 = **65**.

**Ember landing on 70 again, and 70→94 Δ24 surviving verbatim, is a COINCIDENCE**
— an entirely different composition of six lines that happens to total the same.
It is recorded as a coincidence in `research.md` and `DECISIONS.md` so nobody
reads it as continuity. Both were read from `npm run verify` today, population =
the 2 shipped demo stores, not carried forward.

**The fixture rule, written down:** both stores are authored to EXERCISE the
checks — Ember carries a CAPTCHA and a card on file, Neon carries a forced
account, three payment methods that all need a human, and six SKUs with no GTIN.
The totals are whatever falls out. Fitting a fixture to a headline is the thing
the headline is supposed to measure.

## 9 · FILM BEATS THAT CHANGED — what Oscar must re-shoot

**RE-SHOOT:**

1. **`DEMO-SCRIPT.md` 0:25–0:55** — the whole spoken line is rewritten. Old:
   *"three of the six weights are published figures; the other three we allocated
   ourselves."* New: *"six lines, six rows of one published table — 26, 24, 18,
   15, 11, 6."* The second click also changed: `Catalog an agent can read 12/14`
   no longer exists → click **`Product records an agent can read` — 5/6**.
2. **`DEMO-SCRIPT.md` 2:10–2:30 (store B)** — **71 → 65**, and the beat is
   different in kind. *"One point apart"* is **dead**. Neon now fails **three**
   lines (account 0/15, payment 0/11, records 2/6) where Ember fails one.
3. **`DEMO-SCRIPT.md` 2:30–2:45 (close)** — *"nine sources"* → **"twelve
   sources"**; the source panel visibly shows 12 rows.
4. **`FILM-CUES.md` 0:55 READINESS A** — the point list changed to the new line
   names and figures.
5. **`FILM-CUES.md` 1:10 PLATFORM PIVOT** — proof row is now
   *70 → 65 · Ember fails ONE row, Neon fails THREE*.
6. **`JUDGE-60s.md` 0:45–0:60** — Neon **65/100** with three failing lines.

**UNCHANGED, safe to keep:**

- 0:00–0:25 landing — still **70**, still the CAPTCHA VOID stamp.
- 0:55–1:20 co-shop, 1:20–1:55 harness, the share-link beat.
- The CAPTCHA gate beat: **70 → 94, delta exactly 24** survives verbatim.

**NEW beats now available if he wants them:** a third toggle, *"Accept a method
an agent can complete"*, moves the tape **+11** live; a third autopilot fix,
*"Accept a stored-credential method"*; and `prepare_checkout` now returns a
non-blocking **note** when a store clears both walls and still has no route an
agent can complete on.

## 10 · SCREENSHOTS — re-rendered and OPENED, all six

Rendered against `vite preview` on :4177 serving `index-CYN7fJ7h.js` — the same
bundle `npm run build` prints above — then every file opened and read. (They were
first rendered from `index-CVWL9-9p.js`, opened, and re-rendered against the final
bundle after the share-link fix in § 7; all six came back byte-identical in size,
because that fix touches no rendered surface.) The 390px shots are taken inside a **true 390px iframe**,
and the viewport was measured from inside the page rather than assumed:

```
$ chrome --headless --dump-dom .../measure.html | grep -o "INNER_CLIENT_WIDTH=[0-9]*"
INNER_CLIENT_WIDTH=390
```

- `01-landing-1440` — 70, six charged lines, header `100 pts · 100 priced by a published figure · 0 allocated`, the claim box, `Tools the score is measured through … no charge`, **no empty allocated subtotal**
- `02-landing-390` — same at a real 390px, every line wraps, nothing clipped
- `03-merchant-1440` — Ember 70, "What the 30 lost points are", three toggles (payment checked), 12 source rows
- `04-shop-1440` — WILL VOID cites Presenc AI read 2026-08-31; footer now names all six weights
- `05-neon-merchant-1440` — Neon **65**, `0/15 · 0/11 · 2/6`, three autopilot fixes incl. *Accept a stored-credential method*
- `06-shop-390` — mobile shop, unchanged by design

One thing the render caught: the reported line's label truncated to
`Structured tools the score is measured t…` at 1440. Shortened to
`Tools the score is measured through`, rebuilt, re-rendered, re-opened.

## 11 · VERIFIED · NOT VERIFIED · OSCAR'S

**VERIFIED** — the six rows at the source URL, fetched by me in raw HTML today ·
`npm run build` and `npm run verify` exit 0 (pasted) · four tampers proven red
(pasted) · both stores' six lines recomputed outside the product, per store, per
line · the legacy-share-link defect measured at 59 vs 70 · all six screenshots
re-rendered at a measured 390/1440 and opened.

**NOT VERIFIED / HONEST LIMITS**

- Presenc AI is a **vendor page** whose own methodology says its metrics are
  modeled, and it **defines none of its six rows**. Three of our six tests are
  therefore our reading of an undefined label: what "price mismatch vs listed
  feed" means, which payment methods an agent can complete, and what makes page
  structure unambiguous. Each is printed as ours, on the line.
- The 26-point probe's **price half cannot fail on these two fixtures**, because
  the catalog record and the order path read the same field; the half that
  discriminates here is availability. Stated in the rationale, in `research.md`
  and in `orderMath.ts` — and proven non-vacuous by tamper 1.
- **Nothing is deployed.** The live Vercel URL still serves the pre-wave-2 build.
- Nobody who is not Oscar has used this.

**OSCAR'S CLICKS** — deploy · film (see § 9) · submit. Closes **Wed 3 Sep 22:00
CEST**.

## 12 · RE-ENTRY PROMPT — this is what tomorrow starts from

> Repo `~/CODE/tooltruth-webmcp`, **main**. Read `NIGHTRUN-2026-08-31.md` § WAVE 4
> and `DECISIONS.md` § 5e + § 6 (wave 4) before touching the score.
> **State:** the bill is **100 points, all published** — the six charged lines are
> the six rows of Presenc AI's causes table at 26/24/18/15/11/6, and
> `ALLOCATED_POINTS` is 0. Ember & Oak **70/100**, Neon Matcha **65/100**.
> `npm run build && npm run verify` must exit 0 and print
> `ember-oak 70/100 (captcha) · neon-matcha 65/100 (account)`.
> **The claim to protect:** every WEIGHT is published; every TEST is ours. Do not
> let any surface say "the score is published" — `billClaim()` in
> `src/lib/readiness.ts` is the one place that sentence is written.
> **Standing traps:** (1) calling a Zustand getter inside a selector white-screens
> the tab with React #185 — it has shipped twice, `verify-score.mjs` greps for it;
> (2) a weight may be typed in `readiness.ts` only — everywhere else interpolates
> `weightFor()`, and the build fails on a retyped `24 pts` or bare `24%`;
> (3) no figure the product CITES may be printed without a row in
> `src/data/sources.ts` whose URL is quoted in `research.md` (12 rows today);
> (4) `scripts/verify-stores.mjs` recomputes every line outside the product, per
> store — if you change `orderMath.ts` or `REQUIRED_JSONLD_FIELDS`, change its
> twin there too, deliberately, or it goes red for the right reason.
> **Remaining work is Oscar's:** deploy, film the six changed beats in § 9,
> submit. Deadline **Wed 3 Sep 22:00 CEST**.

## 13 · STATE-LOG LINE

`2026-08-31 wave 4 — ReadyCounter L3: the whole 100-point bill is now published. Scored the three remaining rows of Presenc AI's causes table — price mismatch vs feed 18 (the row our 26-point line had been detecting and billing at the wrong price), unsupported payment method 11 (all-or-nothing, agent-completable classification stated as ours), ambiguous page structure 6 (reads back the JSON-LD the page emits, superseding wave 3's ruling in writing) — and folded the three allocated lines away: availability into the 26 row, catalog schema into the 6, the tool surface into a reported line worth zero. Allocated 35 → 0. Refused the seductive 35=35 adjacency and built the checks instead. Source re-fetched in raw HTML by me before writing. Four tampers proven red incl. a budget-neutral one. Caught a bug this build created: legacy ?co= share links would have scored 59 instead of 70. Ember 70/100 (coincidence, different composition), Neon 71 → 65. 12 sources. build + verify exit 0; six screenshots re-rendered at a MEASURED 390/1440 and opened. Six film beats changed, listed for re-shoot. Pushed to main. Oscar owns deploy, film, submit.`

## 14 · RUNTIME + STOP REASON

**Runtime:** 2026-08-31 ~05:30 → ~08:0x CEST.

**Stop reason:** the lane's named build is complete and verified, and the deadline
work left is outward-only. The pitch is now the strong one the brief asked for —
*every point we deduct has a published price, and here is the table* — without the
sentence the brief warned against. Continuing would mean adding features past a
met deadline on the last wave, when what is left is Oscar's three clicks.
