# NIGHTRUN 2026-08-31 — Lane L3, ReadyCounter

## 1 · IDENTITY

| Field | Value |
|---|---|
| Lane | **L3 — ReadyCounter**, WebMCP Challenge ($35K, 4,468 entrants, closes Wed 3 Sep 22:00 CEST) |
| Repo | `~/CODE/tooltruth-webmcp` (`github.com/Morkeeth/tooltruth-webmcp`) |
| Worktree | `~/CODE/tooltruth-webmcp/.worktrees/nightrun-l3` — **not** the live checkout |
| Branch | `nightrun/l3-readycounter`, forked from `origin/main` at `dd90f26`, merged `origin/main` at `3060eea` |
| Commits | `83a8dbb` brand + defensibility · `dbfd0da` docs · `44fbb0b` merge with the integration lane |
| Runtime | 2026-08-30 23:47 → 2026-08-31 00:45 CEST |

**Why a worktree when the brief named this lane the exception.** The brief said
`main`, tree clean. It was not: `git status -sb` at 23:47 showed 5 modified and
6 untracked files, `npm run build` was **red**, and `ps aux` showed two live
`cursor-agent --yolo` processes writing this repo (started 22:08 and 23:42),
with file mtimes landing at the same second I probed. Building in the live
checkout would have clobbered a running agent. That agent has since committed
(`da97b0a`, `3060eea`) and its work is fully merged here.

---

## 2 · STATE — pasted output, not claims

```
$ npm run build
✓ 45 modules transformed.
dist/index.html                   1.41 kB │ gzip:  0.67 kB
dist/assets/index-Df2KIm96.css   19.03 kB │ gzip:  4.16 kB
dist/assets/index-DHntS2qK.js   255.34 kB │ gzip: 78.86 kB
✓ built in 98ms
```

```
$ npm run verify           # exit 0
ok   source parser reads every row the file declares — parsed 8 of 8 declared
ok   point budget sums to 100 — 100/100
ok   every weight names a source that exists — 6 references
ok   every source row carries publisher, figure, url, published, accessed — 8/8
ok   every source URL appears in research.md — 8/8
ok   measured weight price_consistency equals its published figure — weight 26 vs Presenc AI 26%
ok   measured weight agent_checkout_path equals its published figure — weight 24 vs Presenc AI 24%
ok   the manifest count equals the names it lists — WEBMCP_TOOL_COUNT 16 vs 16 names
ok   every tool in the manifest actually registers, and vice versa — 16 tools match
ok   landing screen scores the live store, not a literal — computed at render
ok   no component calls a store getter inside a selector (React #185) — 11 components clean
verify-score: 8 sources, 5 weights, 100 pts — all checks pass

ok   the CAPTCHA delta equals the published figure — 24 pts vs Presenc AI 24%
ok   the lines sum to the printed total — sum 70 vs printed 70
verify-readiness: score 70 with the wall, 94 without, delta 24 — all checks pass

ok   the two stores do not score the same — ember-oak 70 · neon-matcha 57
ok   each store is blocked by a DIFFERENT mechanism — ember-oak captcha · neon-matcha account
verify-stores: ember-oak 70/100 (captcha) · neon-matcha 57/100 (account) — all checks pass
```

**Rendered DOM byte counts, all four views, `--dump-dom` at 00:36:**
`/` 12,312 · `?view=shop` 10,230 · `?view=merchant` 17,513 · `?view=integrations` 5,944.
None is the empty 4,016-byte shell that a crashed React app produces.

---

## 3 · VERIFIED vs 🔴 UNVERIFIED

### VERIFIED — I ran it or looked at it

- `npm run build` exits 0. Output pasted above.
- `npm run verify` exits 0, 6 scripts. Output pasted above.
- **The score checks actually go red.** Tampering `agent_checkout_path` from 24
  to 30 fails two assertions and exits 1. Reintroducing
  `useShopStore((s) => s.getCatalogProducts())` fails the selector check and
  exits 1. Both restored afterwards and re-run green.
- All 5 surfaces rendered in headless Chrome at **1440px** and at a **true
  390px viewport** (inside a 390px iframe — `--window-size=390` clamps to a
  500px viewport on macOS, which made my first "mobile overflow" diagnosis
  wrong). PNGs in `docs/shots/`, each one opened and looked at.
- Ember & Oak **70/100**, 94 with the CAPTCHA cleared, delta **24**.
  Neon Matcha **57/100**, account wall, catalog 5/20. Read from `npm run verify`
  on 2026-08-31, population = the 2 shipped demo stores.
- **16** tools, matching the manifest and `registerTools.ts` name-for-name.
- **8** source rows, every URL quoted in `research.md`.

### 🔴 UNVERIFIED — do not repeat these as facts

- 🔴 **The live deploy at `https://tooltruth-webmcp.vercel.app` was never opened
  by me.** The URL came from the other lane's commit `3060eea`. Every number in
  this file is from a **local** build. Oscar should load the live URL once
  before filming — the merge changed the first screen.
- 🔴 The score's **50 allocated points are a judgement call**, not research. The
  product says so on the line; do not let the pitch imply otherwise.
- 🔴 Presenc AI is a **vendor page** that says its own metrics are modelled. It
  carries two of the five weights. That limit is printed in the UI and in
  `research.md`; it is still the weakest link in the citation chain.
- 🔴 Nobody who is not Oscar has used this. The other lane's EYES panel proposed
  a 72h stranger gate; it has not run.
- 🔴 I did **not** run the WebMCP browser flag path. The judge console path is
  what I exercised.

---

## 4 · BLOCKED ON OSCAR

1. **Film it.** `DEMO-SCRIPT.md` is a one-take 2:45 with every number
   pre-verified and non-overlapping beats. Load the live URL first.
2. **Submit to Devpost.** `DEVPOST.md` is final — no placeholders left.
3. **Veto or keep the brand ruling.** I ruled THE COUNTER / the readiness tape
   in your absence. The three directions and the reasoning are in
   `DECISIONS.md` § Decision 4. Reversing it is one CSS file and one component.
4. **Who owns `main`?** `DECISIONS.md` gave the cloud lane a done-when that
   includes "push main", and this lane's brief authorised the same. Two writers,
   one branch, nothing coordinating them. That is a process bug, not a merge bug.
5. **Decide whether the 24-point account-wall charge is honest enough.** No
   published figure prices an account wall. I charge it the same 24 as a CAPTCHA
   and say so out loud. It is defensible; it is also the sentence a hostile judge
   would pull on.

---

## 5 · DECISIONS I MADE

| Decision | Why | Cost to reverse |
|---|---|---|
| Worked in a worktree, not the live checkout | two cursor-agents were writing it mid-second | none |
| **The tape, not the ring** | a ring is a component-library element and carries one number; a bill carries the arithmetic, which is the product claim | one CSS file + one component |
| **100 pts, 50 measured / 50 admitted** | equal weights silently claimed a missing GTIN costs as much as a CAPTCHA | one constant table |
| Weight = the published figure, enforced by a test | the only way a score stops drifting from its source | delete one check |
| Fixed the Shop-tab crash rather than reporting it | the demo was unfilmable and the deadline is Wednesday | — |
| Made 3 verify scripts assert instead of print | a check that prints `false` and exits 0 is worse than none | — |
| Took **their** README shape, product voice and hack.md | newer, better, and informed by a multi-model panel I did not run | — |
| Kept **my** landing tape over their hero | theirs printed a hardcoded 72 | — |

---

## 6 · NEXT

1. Oscar loads the live URL, films, submits. Everything else is done.
2. If the deploy is stale, redeploy from `main` after this branch lands.
3. Post-submit: the 72h stranger gate the EYES panel proposed. Freeze features.
4. The selector check only covers `src/components`. `src/hooks` and any future
   `src/views` should be added to it.
5. `verify-share` and `verify-integrations` still print booleans and exit 0.
   Same treatment as the other three.

---

## 7 · RE-ENTRY PROMPT

> Repo `~/CODE/tooltruth-webmcp`, branch `nightrun/l3-readycounter` (merged into
> `main` on the night run of 31 Aug). Read `NIGHTRUN-2026-08-31.md`, then
> `DECISIONS.md` §4 (the brand ruling — Oscar may have vetoed it) and §7.
> Run `npm run build && npm run verify`; both must exit 0 and print
> `ember-oak 70/100 (captcha) · neon-matcha 57/100 (account)`.
> Before touching any component, know the standing trap: **calling a Zustand
> getter inside a selector white-screens the tab with React #185** — it has
> shipped twice; `scripts/verify-score.mjs` now greps for it.
> Do not add a figure to the UI unless it has a row in `src/data/sources.ts`
> with a URL and a read date, and that URL is quoted in `research.md`; the build
> fails otherwise. The remaining work is Oscar's: film, submit, and rule on the
> brand. Deadline Wed 3 Sep 22:00 CEST.

---

## 8 · STATE-LOG LINE

`2026-08-31 — ReadyCounter L3: brand ruled (THE COUNTER / receipt tape, ring rejected); score reweighted to 100 pts with 50 measured from Presenc AI and 50 labelled as ours; sources.ts + verify-score make every printed figure traceable and fail the build on drift (proven red); found and fixed a pre-existing React #185 white-screen on the Shop tab that had killed the co-shop demo in main while every test passed, plus the same bug in 3 more components after merging the integration lane; 3 verify scripts that printed "false" and exited 0 now assert. build + verify exit 0; ember-oak 70/100, neon-matcha 57/100, 16 tools, 8 sources. Merged the integration lane's 16 tools/autopilot/import. Oscar owns film + submit.`

---

## 9 · RUNTIME + STOP REASON

**Runtime:** 2026-08-30 23:47 → 2026-08-31 00:45 CEST (~58 min).

**Stop reason:** the lane is done. Brand ruled, built, rendered and inspected;
the score is traceable and its checks are proven red as well as green; the
merge with the concurrent lane is complete and green; every doc is final except
the two clicks that are Oscar's by law. Continuing would mean either building
past a deadline that is already met or taking an outward act I am not allowed to
take.

**Three things that cost the most time, recorded so the next run does not pay
them again:**

1. `timeout` does not exist on macOS. Every `timeout N chrome …` returned
   "command not found" and I read the resulting 0 bytes as "the page is broken".
   `/tmp/tmo` in this run is a two-line replacement.
2. `pkill -f "vite preview"` killed the server, after which Chrome hung forever
   on a dead port — and I read that as "`origin/main`'s landing page hangs",
   which was **false**. A static HTML file hung the same way; that is what
   showed it was the harness, not the app. **Retracted in full.**
3. `--window-size=390` gives a 500px viewport on macOS. The "mobile overflow"
   I first diagnosed did not exist. Measuring `documentElement.clientWidth`
   settled it in one command; guessing at CSS had already cost three.
